var pluginName = 'simple-analytics';

var noOp = function noOp() {
  return console.log("No ".concat(pluginName, " node implementation"));
};

function simpleAnalyticsPlugin(userConfig) {
  // Allow for userland overides of base methods
  return {
    NAMESPACE: pluginName,
    config: {},
    initialize: noOp
  };
}

/* global sa */

var index = simpleAnalyticsPlugin;

export default index;
